# Конфиг хост-версии (PM2 + хост-Postgres). Скопируй в .env и подставь реальные значения.
# Контейнерный стек настраивается отдельно: .env.example в корне проекта.
DATABASE_URL=postgres://user:password@127.0.0.1:5432/pixisnake
